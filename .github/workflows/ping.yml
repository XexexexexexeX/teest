name: Ping Render Server

on:
  schedule:
    - cron: '*/6 * * * *' # Каждую минуту
  workflow_dispatch: # Добавьте этот триггер для ручного запуска

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Send Ping Request
        run: |
          curl -X GET https://qew-xnec.onrender.com/ping
